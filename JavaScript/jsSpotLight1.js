#pragma strict

/*var duration : float = 1.0;
var color0 : Color = Color.yellow;
var color1 : Color = Color.blue;
*/
function Start () {
//color0 = Color.yellow;
//color1 = Color.blue;
}

function Update () {
	// set light color
/*	var t : float = Mathf.PingPong (Time.time, duration) / duration;
	transform.light.color = Color.Lerp (color0, color1, t);
	transform.light.intensity = Mathf.PingPong (Time.time / 3, duration) / duration * 2;
*/
}